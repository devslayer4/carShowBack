spring.application.name=CarShowBack
spring.datasource.url=jdbc:postgresql://localhost:5432/CarShow
# configure the authentication to the database
spring.datasource.username=postgres
spring.datasource.password=Barth123
# hibernate does not create or update automatically the schema (tables, attributes, etc)

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.generate-ddl=true
spring.jpa.show-sql=true